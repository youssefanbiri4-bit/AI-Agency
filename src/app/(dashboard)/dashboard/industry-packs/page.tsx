import { IndustryPacksClient } from './IndustryPacksClient';

export const metadata = {
  title: 'Industry Packs - AgentFlow AI',
  description: 'Planning-only industry starter packs for campaigns, workflows, prompts, and creative ideas.',
};

export default function IndustryPacksPage() {
  return <IndustryPacksClient />;
}
