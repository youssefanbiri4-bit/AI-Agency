import { LoadingState } from '@/components/ui/LoadingState';

export default function AlexLoading() {
  return (
    <LoadingState
      title="Loading Alex"
      description="Connecting to your AI assistant."
    />
  );
}
