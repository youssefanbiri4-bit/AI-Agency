import { LoadingState } from '@/components/ui/LoadingState';

export default function ContentStudioLoading() {
  return (
    <LoadingState
      title="Loading Content Studio"
      description="Preparing content items and publishing views."
    />
  );
}
