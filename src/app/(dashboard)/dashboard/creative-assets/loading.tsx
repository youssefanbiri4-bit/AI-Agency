import { LoadingState } from '@/components/ui/LoadingState';

export default function CreativeAssetsLoading() {
  return (
    <LoadingState
      title="Loading Creative Assets"
      description="Preparing asset library and media files."
    />
  );
}
